<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.dao.ConsumeDao">
	
	<select id="search" parameterType="string" resultType="consume">
		select * from consume where id= #{id}
	</select>

	<insert id="insert" parameterType="consume">
		insert into consume (id, code, eatdate, count)
		values (#{id:VARCHAR}
		,#{code:INTEGER}
		,#{eatdate:VARCHAR}
		,#{count:INTEGER})
	</insert>
   
   <delete id="delete" 		parameterType="int">
   		delete from consume where num=#{num}
   </delete>
   
   <update id="update"		parameterType="consume">
   		update consume set id = #{id:VARCHAR}
   		        		 ,code = #{code:INTEGER}
   		       			 ,eatdate= #{eatdate:VARCHAR}
   		      			 ,count = #{count:INTEGER}
   		where  num = #{num:INTEGER}
   </update>
   
</mapper>


