<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="userfood" >
   <insert id="insert"		parameterType="userfood">
   		insert into usereat (no, id, code)
   		values ( #{no:INTEGER}
   		        ,#{id:VARCHAR}
   		        ,#{code:INTEGER})
   </insert>
   <delete id="delete" 		parameterType="int">
   		delete from usereat where code=#{code}
   </delete>
   <update id="update"		parameterType="userfood">
   		update user set id=#{id:VARCHAR}
   		                , code = #{code:INTEGER}
   		where  no = #{no:INTEGER}
   </update>
   
   <select id="search"		 parameterType="string"    resultType="food">
   		select *
   		from food
   		where code in (select code from usereat where id = #{id})
   </select>
   
   <select id="searchAll"	parameterType="pagebean"	resultType="userfood">
   		select * from usereat 
   		<!-- 동적 쿼리시 조건에 의해 where 나 and를 붙여주는 태그  -->
   		<where>
   			<if test='key != null and key!="all"'>
   				<if test=" word != null">
   					<choose>
   						<when test="key == 'id'">
   							id = #{word}
   						</when>
   						<when test="key == 'address'">
   							address like concat('%',#{word},'%')
   						</when>
   					</choose>
   				</if>
   			</if>
   		</where>
   		order by id
   </select>
   <select id="count" parameterType="string" resultType="int">
   		select MAX(no) as cnt from usereat where id = #{id}
   </select>
</mapper>